# Secret para valores sensíveis da aplicação
# Em produção real, use Sealed Secrets ou External Secrets Operator
apiVersion: v1
kind: Secret
metadata:
  name: site-kubectl-secret
  namespace: reliabilitylab
  labels:
    app.kubernetes.io/name: site-kubectl
    app.kubernetes.io/part-of: reliabilitylab
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  # Placeholder — a aplicação atual não exige segredos obrigatórios
  # Pronto para futuras integrações (tokens, chaves de API, etc.)
  APP_SECRET_KEY: "altere-este-valor-em-ambientes-reais"
