apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-content-api
  namespace: chaos-mesh
spec:
  mode: all
  selector:
    namespaces:
    - production
    labelSelectors:
      app: content-api
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: "120s"
